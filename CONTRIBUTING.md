# 贡献指南

感谢你为 SMC-Web-Agent 贡献代码。为了让协作顺利、代码长期可维护，请在提交前阅读并遵守以下约定。

## 一切从简

这是本项目最核心的一条原则：**写最简单、最直接的代码，避免复杂、冗余或暂时用不到的抽象与逻辑。**

- 只为当前真实的需求写代码，不为「以后可能用到」的假设性场景预留扩展。
- 不要引入多余的分层、设计模式、工厂、策略、兼容分支，除非问题本身确实需要。
- 不写用不到的参数、字段、配置项和死代码；加了就说明为什么需要。
- 优先用项目里已有的做法和工具，重复三行的相似代码，好过为它新建一个过早的抽象。
- 一个 bug 修复不需要顺带重构无关代码；一次性的操作不需要写成通用工具。

判断标准：**如果删掉它不影响现在能跑通的功能，那就别写。** 简单是质量的一部分，不是偷懒。

## 代码可读性

代码是给人读的，其次才是给机器执行的。

- 命名要能自解释：函数、变量、类型名说清楚「是什么」，让读者无需翻实现也能理解意图。
- 注释只写「为什么」，不写「是什么」——凡是能从命名和代码本身看出来的，都不需要注释。隐藏约束、不易察觉的坑、绕开某个 bug 的 workaround，才值得一行注释。
- 保持改动小而聚焦、单一职责；一次提交只做一件事，方便 review 和回退。
- 中文网站的 UI 文案用中文，但代码里的变量名、函数名、注释、类型、入库数据一律英文。品牌名「SMC-Web-Agent」保留英文。

## 测试与迁移

后端测试用 Python 标准库 `unittest`，在 `server/` 目录下运行：

```bash
uv run python -m unittest discover -s tests                 # 全部测试
```

数据库迁移用 Alembic，配置文件为 `server/alembic.ini`；容器化日常由 `./dev.sh dev-migrate`（migrate 服务执行 `run_migrations.py`）应用迁移，生成新迁移在 `server/` 下运行：

```bash
alembic revision --autogenerate -m "迁移说明"   # 生成新迁移（生成后需人工审阅）
alembic upgrade head                            # 应用
alembic downgrade -1                            # 回滚一步
```

## 提交前

- 前端/后端改动后，自己先跑通编译与静态检查（TypeScript 检查、lint、后端 `unittest`），不要留下编译不过或半成品的中间态。
- 涉及生产环境/数据结构的改动，务必先读 `docs/deployment.md` 里的「数据安全红线」。
- 改动尽量小、可回退，不要用兼容分支长期掩盖旧实现。

## 更多文档

- `docs/deployment.md`：开发栈与生产栈的配置、日常操作、部署（含数据安全红线）。
- `docs/architecture.md`：整体架构与飞书同步链路。
- `docs/feishu-integration.md`：飞书应用权限清单与凭据配置。
- `src_doc/`：核心模块实现原理（人员主数据合并、组会预告推送、周报统计）。
